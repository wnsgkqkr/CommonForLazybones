<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
        namespace="com.cfl.mapper.UserMapper">
    <insert id="insertUser" useGeneratedKeys="true" keyProperty="userSequence">
        insert into user ( user_id, user_type, tnnt_id, svc_nm )
        values (#{user.userId}, #{user.userType}, #{user.tenantId}, #{user.serviceName})
    </insert>

    <select id="getUser" resultType="User">
        select *
        from user
        where svc_nm = #{user.serviceName} and tnnt_id = #{user.tenantId} and user_id = #{user.userId}
    </select>
</mapper>

