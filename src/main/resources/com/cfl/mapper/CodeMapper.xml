<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
        namespace="com.cfl.mapper.CodeMapper">

    <resultMap id="Code" type="Code">
        <id property="codeId" column="cd_id" />
        <result property="parentCodeId" column="prnt_cd_id"/>
        <result property="multiLanguageCode" column="mlng_cd"/>
        <result property="codeName" column="cd_nm"/>
        <result property="codeDescription" column="cd_desc"/>
        <result property="isUsed" column="use_yn"/>
        <result property="sortOrder" column="sort_ord"/>
        <result property="tenantId" column="tnnt_id"/>
        <result property="serviceName" column="svc_nm"/>
    </resultMap>

    <select id="selectCode" resultMap="Code">
        SELECT
            *
        FROM
            cfl_code
        WHERE
            cd_id = #{codeId}
            AND svc_nm = #{serviceName}
            AND tnnt_id = #{tenantId}
    </select>

    <insert id="insertCode">
        INSERT INTO
            cfl_code (cd_id, mlng_cd, prnt_cd_id, tnnt_id, cd_desc, use_yn, sort_ord, cd_nm, svc_nm)
        VALUE
            (#{codeId}
            , #{multiLanguageCode}
            , #{parentCodeId}
            , #{tenantId}
            , #{codeDescription}
            , #{isUsed}
            , #{sortOrder}
            , #{codeName}
            , #{serviceName})
    </insert>

    <update id="updateCode">
        UPDATE
            cfl_code
        SET
            mlng_cd = #{multiLanguageCode}
            , prnt_cd_id = #{parentCodeId}
            , tnnt_id = #{tenantId}
            , cd_desc = #{codeDescription}
            , use_yn = #{isUsed}
            , sort_ord = #{sortOrder}
            , cd_nm = #{codeName}
            , svc_nm = #{serviceName}
        WHERE
            cd_id = #{codeId}
    </update>

    <delete id="deleteCode">
        DELETE FROM
            cfl_code
        WHERE
            cd_id = #{codeId}
            AND svc_nm = #{serviceName}
            AND tnnt_id = #{tenantId}
    </delete>

</mapper>

